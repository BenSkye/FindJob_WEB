export interface ITemplate {
    _id: string;
    name: string;
    thumbnail: string;
    htmlStructure: string;
    cssStyles: string;
    isActive: boolean;
    createdAt: string;
    updatedAt: string;
}